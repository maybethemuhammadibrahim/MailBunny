/* DraftPanel.jsx
 * ---------------------------------------------------------------
 * AI Draft assistant panel shown in the email detail view.
 * Displays the AI-generated reply in an editable textarea with
 * Send, Edit, and Regenerate buttons. Phase 8 will implement
 * the full functionality.
 * --------------------------------------------------------------- */


/**
 * DraftPanel renders the AI draft sidebar within the email view.
 * Placeholder — will receive props (draftText, confidenceScore,
 * onSend, onRegenerate, onEdit) in Phase 8.
 *
 * @returns {JSX.Element} the AI draft panel component
 */
function DraftPanel() {
  return (
    <div className="flex-1 flex flex-col p-4">
      <p className="text-[14px] text-on-surface-variant">
        DraftPanel placeholder — Phase 8
      </p>
    </div>
  );
}

export default DraftPanel;
